name: Release Drafter (git-cliff)

on:
  push:
    branches:
      - main
      - lts

permissions:
  contents: write
  pull-requests: read

jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Читаем всю историю коммитов и тегов

      - name: Generate Changelog with git-cliff
        id: git-cliff
        uses: orhun/git-cliff-action@v4
        with:
          config: .github/cliff.toml
          args: --unreleased --bump --github-repo ${{ github.repository }} --github-token ${{ secrets.GITHUB_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Безопасное логирование без ошибок bash
      - name: Prepare Tag Name and Log Output
        id: tag
        env:
          CHANGELOG_TEXT: ${{ steps.git-cliff.outputs.content }}
        run: |
          RAW_VER="${{ steps.git-cliff.outputs.version }}"
          echo "=== GIT-CLIFF VERSION: $RAW_VER ==="
          echo "=== GIT-CLIFF CHANGELOG CONTENT ==="
          echo "$CHANGELOG_TEXT"
          echo "==================================="
          
          if [[ "$RAW_VER" == v* ]]; then
            echo "version=$RAW_VER" >> $GITHUB_OUTPUT
          else
            echo "version=v$RAW_VER" >> $GITHUB_OUTPUT
          fi

      - name: Create or Update GitHub Release Draft
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          tag_name: ${{ steps.tag.outputs.version }}
          name: ${{ steps.tag.outputs.version }}
          body: ${{ steps.git-cliff.outputs.content }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}